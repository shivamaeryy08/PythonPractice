x = 0

if x == 0:
    x = "meow"
# Python is dynamically typed meaning that a variables data type can be changed via changing its content

# Strongtly typed meaning holds onto data type, meaning cant do power of 2 on a string

# Python is strong, dynamically typed

#
